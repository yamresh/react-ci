language: node_js
node_js:
  - "latest"
cache:
  yarn: true
  directories:
    - node_modules
script:
  - yarn test
  - yarn test:coverage
  - yarn build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: build
  keep_history: true
  on:
    branch: master
notifications:
  email:
    - amreshy29@gmail.com
